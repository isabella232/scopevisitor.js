require('fs').readFileSync;/*REF:exports.readFileSync,,@node*/

module.id;/*REF_DISABLED(we can only get the full stdlib path through the type name, which does not work for non-function refs:module.id,,@node*/

require/*REF:exports.require,,@node*/;
